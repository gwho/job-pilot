{
  "title": "JobsDB HK Actor Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Search query",
      "type": "string",
      "description": "Job title or keywords to search for on JobsDB HK.",
      "editor": "textfield"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location filter. Leave empty to search all of Hong Kong.",
      "editor": "textfield",
      "default": ""
    },
    "maxItems": {
      "title": "Maximum results",
      "type": "integer",
      "description": "Maximum number of job listings to scrape (search results + detail pages). Keep at 10 for v1.",
      "default": 10,
      "minimum": 1,
      "maximum": 50
    }
  },
  "required": ["query"]
}
