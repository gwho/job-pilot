{
  "title": "JobsDB HK Actor Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Search query",
      "type": "string",
      "description": "Job title or keywords to search for on JobsDB HK.",
      "editor": "textfield"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Location filter. Leave empty to search all of Hong Kong.",
      "editor": "textfield",
      "default": ""
    },
    "maxItems": {
      "title": "Maximum results",
      "type": "integer",
      "description": "Maximum number of unique job listings to scrape in total across all pages.",
      "default": 10,
      "minimum": 1,
      "maximum": 100
    },
    "maxPages": {
      "title": "Maximum search result pages",
      "type": "integer",
      "description": "How many pages of search results to paginate through. Each page yields ~10 jobs. Increase with maxItems for larger searches.",
      "default": 1,
      "minimum": 1,
      "maximum": 10
    }
  },
  "required": ["query"]
}
