# AI Discussion Topics — Remember Restore Context

Use these prompts for deeper review:

1. Explain why a restore workflow needs both a strict allowlist and project-specific exceptions.
2. Walk through why the nine `context/` files are ordered from project overview to progress tracker.
3. Compare restoring from `memory.md` alone versus restoring from `memory.md` plus canonical project context.
4. Review how an allowlist can prevent accidental secret exposure while still allowing enough context for safe implementation.
